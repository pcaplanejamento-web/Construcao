# Catálogo de Componentes

Todos os componentes estendem
[`BaseElement`](../src/components/base-element.js) (Shadow DOM, `emitir()`,
limpeza de inscrições). Convenção: **dados descem por props/atributos; eventos
sobem por `CustomEvent`**.

---

## Primitivos reutilizáveis — `src/components/` (sem regra de negócio)

| Componente | Atributos / Propriedades | Eventos | Descrição |
|------------|--------------------------|---------|-----------|
| `ui-button` | `variant`, `loading`, `disabled`, `tamanho`, `full` | `click` (nativo) | Botão. Slot = conteúdo. Variantes: `primario` (**gradiente** `--grad-primaria` + sombra colorida), `secundario` (branco + sombra leve), `tonal` (verde-claro/`--cor-primaria-suave` — ações suaves, ex.: Editar), `perigo` (vermelho sólido), `perigo-contorno` (outline vermelho — ex.: ícone excluir), `fantasma`. Lift no hover; **animação de clique** (no `:active` afunda ~4% + a sombra "fecha" num halo curto, padrão p/ todos); raio `--raio-md`. **Foco por teclado**: anel `:focus-visible` (não aparece no clique de mouse; `perigo`/`perigo-contorno` usam anel vermelho). |
| `ui-input` | `label`, `name`, `type`, `value`, `placeholder`, `error`, **`required`**, `step`, `min`, **`formato`**; props `.value`, `.numero` | `input`, `change`, `enter` | Campo com rótulo e erro (`role="alert"`). **`required`** mostra um **asterisco** (`*`, vermelho) no rótulo. **O `error` é INCREMENTAL** — mostra/atualiza/remove a mensagem **sem re-renderizar** o campo, então **não apaga o texto já digitado** nem o cursor (a borda vermelha vem de `:host([error])`). **`type="password"`** ganha automaticamente um botão de **olho** (revelar/ocultar a senha). **`formato`** (opt-in — máscara ao digitar + `inputmode` p/ o teclado certo no celular): **`moeda`** → mostra "R$ 1.234,50" (acumulador de centavos), `inputmode="decimal"`; **`.value` continua devolvendo o número puro** ("1234.5", p/ `Number(el.value)` seguir funcionando) e `.numero` devolve `Number`; aceita `value` inicial em reais (`"1500.00"`→"R$ 1.500,00"). **`telefone`** → `(00) 00000-0000`; **`cnpj`** → `00.000.000/0000-00` (ambos `inputmode="numeric"`; `.value` = texto mascarado). Sem `formato`, comportamento idêntico ao anterior. **Mobile/iOS:** o `<input>` usa `font-family: inherit` (fonte do app, não Arial do Shadow DOM) e **`font-size: 1rem` (16px)** — evita o **zoom automático do iOS** ao focar o campo (o iOS amplia a página se o controle tem fonte < 16px). Vale p/ os `<textarea>` dos forms também. |
| `ui-select` | `label`, `name`, `value`, `placeholder`, `error`, opcional **`criar`**; prop `.options=[{value,label,editavel?,removivel?}]`, `.value` | `change` ({value,name}), **`criar`**, **`editar`**, **`excluir`** ({value}) | **Combobox PESQUISÁVEL** — o dropdown padrão de TODO o sistema (consolidado: o antigo `ui-search-select` foi **removido**, sem redundância). Busca (filtra por rótulo), teclado (↑/↓/Enter/Esc), alvos ≥44px. **Painel SOBREPOSTO** (`position:fixed`, **portado ao `document.body`**) → **não altera largura/altura** do container nem é recortado por modais; reposiciona ao rolar/redimensionar, fecha ao clicar fora / Esc / Tab. **Seta**: aponta p/ **baixo fechado**, **cima aberto**, clicável em qualquer estado (abre/fecha mesmo após a seleção). **Ícones por opção**: se a opção traz `editavel`/`removivel` mostra **editar**/**excluir** (SVG) e emite os eventos correspondentes — o pai decide (ver `drop-crud`). Retrocompatível (aceita `value` por atributo). Com `criar="X"` mostra **"+ X"** + evento `criar`. **`ajuda="termo"`** (ou `ajuda-titulo`/`ajuda-texto`) põe um **`<ui-ajuda>`** ("?") ao lado do rótulo — sem `ajuda`, nada muda. **`required`** = asterisco (`*`) no rótulo + `aria-required` no campo. |
| `ui-ajuda` | **`termo`** (busca no glossário) **ou** `titulo`+`texto` (livre) | — | Botão **"?"** (24px, circular) que abre um **popover curto** explicando um termo de domínio. **Portado ao `document.body`** (`position:fixed`, mesmo padrão do `ui-select`) → não é recortado por modais nem empurra o layout; reposiciona ao rolar/redimensionar; fecha ao clicar fora / Esc / clicar de novo. Acessível (`aria-label`, `aria-expanded`, balão `role="dialog"`). Conteúdo dos termos vem de [`src/features/shared/glossario.js`](../src/features/shared/glossario.js) (fonte única: Oferta, Cotação, Orçamento, Ofertante, Responsabilidade, Transferência×Pagamento, Categoria, "O que registrar"). |
| `ui-switch` | `checked`, `disabled`, `label`; prop `.checked` | `change` ({checked}) | Interruptor (toggle). Alvo de toque ≥44px. |
| `ui-modal` | `open`, `title`, `largo`; slots: default, `rodape` | `fechar` | Diálogo overlay (X, backdrop, Esc). Atributo **`largo`** = diálogo mais largo (max-width 840px; ex.: leitura de e-mail). **Superfície "liquid glass"** (`.dialogo` = `--vidro-fundo-forte` + `--vidro-blur` + `--vidro-borda` + `--vidro-realce`); o `.backdrop` ganha `blur(4px)` leve. Animação `surgir` respeita `prefers-reduced-motion`. **Fecha também ao trocar de rota** (`rotamudou`) — um link interno (ex.: "abrir página do item") não deixa banners flutuantes sobrepostos. **Rola só na vertical** (`.corpo` `overflow-y:auto; overflow-x:hidden; touch-action:pan-y`; `.dialogo overflow:hidden`); largura/altura adaptativas (`max-height:90dvh`) com `env(safe-area-inset-*)` (iOS). **Foco (a11y)**: ao abrir foca o diálogo, **prende o Tab** dentro dele (loop 1º↔último, atravessando shadow DOM) e **devolve o foco** ao elemento anterior ao fechar; o trap **só age quando o foco já está dentro deste modal** → seguro p/ modais **empilhados** (o de fundo não rouba o foco do de cima). `X` com `:focus-visible`. |
| `ui-toast` / `toast-host` | `tipo`, `message`; prop `.acao={rotulo,onAcao}` | `dispensar` | Notificações; host ouve o `event-bus`. Superfície **liquid glass** (`--vidro-fundo-forte` + `--vidro-blur` + borda hairline; borda-esquerda colorida por tipo); animação `entrar` respeita `prefers-reduced-motion`. **Toast com ação** (`toastAcao(msg, rotulo, onAcao)` do event-bus): mostra um botão (ex.: **"Desfazer"**), dura ~6s; clicar roda `onAcao` e fecha o toast. Usado no **desfazer de exclusões** (soft-delete) via `drop-crud` → `dataStore.reativar*`. |
| `ui-card` | `title`, **`mesa`**, **`acao-fixa`**; slots: default, `acoes`, `rodape` | — | Cartão de superfície. **Padrão:** o botão de adicionar da tabela vai no `slot="acoes"` (cabeçalho, colado à direita); título longo **quebra** mantendo o botão à direita. **`acao-fixa`** (opt-in) — **no mobile (≤820px)** o cabeçalho (título + ação primária) **gruda no topo** da rolagem (`position:sticky`; exige `.card overflow:visible`, aplicado só nessas telas), mantendo o "+ Registrar/Novo…" sempre alcançável em listas longas; sem o atributo nada muda (não-quebra). Atributo **`mesa`** = vira "mesa" (corpo em `--vidro-mesa` + **`--sombra-mesa`** moderna + borda `--vidro-borda`) p/ destacar os cards sobre ela; usado em containers de **tabela/grade** (ex.: "Mesa com despesas"). A mesa **não** levanta no hover. **Superfície "liquid glass"**: o `.card` usa `--vidro-fundo` (quase opaco) + `--vidro-blur` + `--vidro-borda` + `--vidro-realce` (sheen) + `--sombra-md` (fallback opaco sem `backdrop-filter`). Os cartões de KPI (`--grad-*`) e os controles continuam **sólidos**. |
| `ui-data-table` | `.columns` (`{chave,titulo,formato?,alinhar?,largura?,secundaria?,`**`moeda?`**`,`**`valorNum?(linha)`**`}`; `secundaria`=some no mobile ≤820px; **`moeda:true`**=entra no somatório; **`valorNum`**=número a somar p/ total derivado, default `Number(linha[chave])`), `.rows`, `.acoes`, **`.acoesMassa`** ([{nome,rotulo,variant?}]); attrs `empty-text` (ou o estado vazio RICO **`empty-titulo`**+**`empty-texto`**+**`empty-icone`** → renderiza um `ui-empty-state`), `fluido`, `clicavel`, **`editar-massa`**, **`excluir-massa`** | `acao`, `linha`, **`selecao`** ({linhas}), **`editar-massa`**/**`excluir-massa`** ({linhas}), **`acao-massa`** ({acao,linhas}) | **Data-grid** (não mais passivo): **coluna de seleção fixa à esquerda** (+ Selecionar Todos) — **só aparece se houver `editar-massa`/`excluir-massa`** (a seleção serve às ops em massa; tabela só-leitura não a mostra); **cabeçalho clicável** abre `ui-coluna-menu` (ordena/filtra por Valores, estado interno que **persiste**); **`_texto` resolve o texto VISÍVEL via DOM** (com cache) — pega o rótulo de `category-badge`/`ui-badge` (atributo `nome`/`text`) e ignora decorativos (`aria-hidden`), então **filtro/ordenação/busca/export funcionam em TODAS as colunas, inclusive Classificação/Categoria**; **busca global** via `buscar(texto)` — a `ui-busca` é auto-injetada no **cabeçalho do `ui-card` mais próximo** (slot `acoes`, à ESQUERDA do botão de ação, ou no lugar dele); só atualiza o corpo p/ preservar foco/texto; **linha de Totais** (`<tfoot>` **sticky-bottom FIXA**, soma das colunas `moeda` visíveis) — colada na base da rolagem (acima da barra horizontal), **opaca** (z-index alto), **no meio E no fim do scroll** (trio: `table` `margin-bottom:-esp-2` + `.wrap` `padding-bottom:esp-2` + `tfoot td` `translateY(+esp-2)` p/ BAIXO, que absorve o `border-spacing` que sobra após o `tfoot` — sem o gap de 8px no fim e sem cortar/vazar no meio; o Chrome ignora margem negativa p/ a área rolável, daí o trio); **cabeçalho sticky-top**; **barra de seleção** com a SOMA dos selecionados + **"Editar selecionadas"** (se `editar-massa`) e **"Excluir selecionadas"** (se `excluir-massa`). `.wrap` rolável (`max-height:70vh`). Células `.dir` em Space Grotesk 700. **Linha-card:** as células de **dados** formam **um card branco com contorno contínuo** (`border-collapse:separate`+`border-spacing`; borda `1px --cor-borda` em retângulo único — sem bordas internas; cantos arredondados). **Hover = no COMPONENTE INTEIRO (nunca por célula):** **sombra única via `tbody tr::after`** (`--sombra-realce`, sutil; sobe o mesmo `translateY(-4px)` das células → alinhada ao contorno) que cobre **só a região de dados** (`left:2.5rem`(=largura da marcação) quando há marcação — `:host([tem-selecao])`; `clip-path: inset(-24px -24px -24px 0)` corta o vazamento da sombra **exatamente na borda esquerda do card**, deixando a marcação limpa) e fica em `z-index:4`. **A coluna de marcação (.sel) tem largura FIXA (2.5rem) e NÃO é sticky** — a linha inteira (marcação + card + conteúdo) rola como UMA peça na horizontal, então sombra/contorno nunca desalinham na rolagem — **por cima da marcação e da linha de soma**, abaixo do cabeçalho (z5/6). Lift nas células de dados (`translateY(-4px)`) + contorno realçado (`--cor-borda-forte`). **Tópicos (thead), linha de soma (tfoot, sem divisória) e coluna de marcação (.sel)** usam `--cor-mesa` — a marcação fica **separada e NÃO anima**. Vale p/ TODAS as tabelas (inclui `despesa-table`/`users-table`, que reusam este componente). **Mobile (≤600px): cada linha vira CARD empilhado** — `thead` some; cada `td` é `display:flex` "rótulo ↔ valor" (via `data-label`/`::before`), divisória sutil + feedback de toque (`:active`); **mostra só as colunas PRINCIPAIS** (as `.sec` ficam ocultas — card conciso; toque na linha abre o detalhe); **seleção = 1ª linha do card** ("Selecionar" + checkbox, em fluxo, **sem sobrepor**); ações em linha cheia; **checkbox tem alvo de toque ≥34px** (envolto num `label.selbox`, sem mudar o visual); **linha de TOTAIS encosta na última (sem fresta)** — desktop puxa o `tfoot` sticky pelo `border-spacing` (`transform: translateY(-esp-2)`); **sem scroll horizontal, sem hover**. **Barra EXPORTAR** (`.barra-exportar`, canto inferior ESQUERDO, **logo após o "Total"**): botões **XLS · XLSX · CSV · PDF** que baixam a mesa via `exportar-tabela.js` — exporta as **linhas VISÍVEIS** (filtro/busca/ordenação aplicados), só colunas com título (ignora ações/ícones), título do arquivo = `title` do `ui-card` mais próximo. **`_textoExport(c,linha)`** extrai o texto REAL via DOM (≠ `_texto` regex interno): pega o rótulo de **badges** (atributo `nome`/`text` do `category-badge`/`ui-badge`) e **ignora decorativos** (`aria-hidden`, ex.: iniciais do avatar) — assim Classificação/Categoria/Empresa saem corretas no arquivo. No mobile os botões viram alvos de toque ≥44px em linha cheia. Vale p/ TODAS as mesas do sistema. |
| `ui-coluna-menu` | `.coluna`, `.valores` (textos distintos), `.estado` ({ordem,selecionados}), `.ancora` (DOMRect) | `aplicar` ({ordem,selecionados}), `remover`, `fechar` | Dropdown ÚNICO de cabeçalho (popover fixo **colado ao tópico**: borda-topo na cor primária, raio só embaixo + backdrop/Esc): **Crescente/Decrescente**, **Filtro** (busca), **Selecionar Todos** + checklist. **Scroll vertical interno** na lista (`overflow-y:auto` + `flex:1`/`min-height:0`; cada `.item` é `flex:none` **min-height 28px** — sem isso os itens comprimiam e a lista não rolava). `posicionar()` mede a altura REAL e **sobe/cola** o painel p/ o rodapé (Aplicar) nunca sair da tela. Mobile/touch: painel quase full-width, itens e checkbox maiores. Aberto pelo `ui-data-table` em todas as tabelas — filtra/ordena QUALQUER coluna, **inclusive Classificação/Categoria** (o `_texto` resolve o rótulo dos `category-badge`). |
| `ui-busca` | attr `placeholder`; método `definir(texto)`; export `injetarBuscaNoCard(host, tabela)` | `buscar` ({texto}) | Botão de **pesquisa** (ícone `busca`) que expande o campo da **direita p/ a esquerda**, sobreposto (overlay) ao conteúdo à esquerda **sem deslocar nada**. Componente ÚNICO reusável. `injetarBuscaNoCard` coloca a lupa no **cabeçalho do `ui-card`** (slot `acoes`, à esquerda do botão) ligada a `tabela.buscar()`; o `ui-data-table` chama isso sozinho — wrappers (`despesa-table`, `users-table`) chamam com a tabela interna por estarem em outro shadow. Fecha ao perder foco vazio / Esc. |
| `ui-icon` | `name`, `size` | — | Biblioteca de ícones padrão (SVG `currentColor`). Sem emoji. Inclui `fornecedor`, `contato`, `cotacao` (módulo Compras), `email`, `usuarios`, `seta-direita` (login). |
| `ui-alert` | `tipo` (erro\|aviso\|info\|sucesso), `message`; prop `.mensagem` | — | **Componente PADRÃO de mensagem de erro/alerta inline.** Some quando sem `message`. Usar sempre que houver mensagem de erro de validação numa tela/form. |
| `ui-tabs` | `.abas=[{id,rotulo,icone}]`, attr `ativo`; evento `mudar` | Abas com slots nomeados (`slot="<id>"`); mostra só a aba ativa. A aba ativa muda **apenas a cor** (texto + ícone via `currentColor`) e a barra inferior — sem alterar `font-weight`/tamanho (evita reflow/deslocamento). **Lembra a aba ativa por rota** (sessionStorage, chave = pathname + ids das abas) → ao sair e voltar, reabre na mesma aba. **No mobile a barra rola SÓ na horizontal** (`overflow-x:auto; overflow-y:hidden; nowrap; touch-action:pan-x`; scrollbar oculta) — as abas nunca quebram, estouram nem deslizam na vertical. **Indicador de rolagem**: um **degradê** aparece na borda direita/esquerda quando há abas fora da vista (some no início/fim); a **aba ativa** entra na vista automaticamente (`scrollLeft`, sem mexer na rolagem vertical da página). **Swipe entre abas (só toque):** arrastar na horizontal no `.painel` vai p/ a aba anterior/próxima (sem loop nas pontas). Usa eventos de PONTEIRO de toque no `.painel` → as linhas de `ui-lista-gestos` (que dão `stopPropagation` no `pointerdown` do arraste) **vencem**: swipe na linha edita/exclui e NÃO troca de aba; swipe fora da lista troca. Ignora quando começa sobre um conteúdo que rola na horizontal (tabela/gráfico). |
| `ui-badge` | `color` (hex ou `var(--token)`), `text` | — | Etiqueta colorida; fundo via `color-mix` (tema-seguro). |
| `ui-spinner` | `text`, `centro` | — | Indicador de carregamento. |
| `ui-empty-state` | `icone`, `titulo`, `texto`; slot `acao` | — | Estado vazio. |

---

## Componentes de domínio — `src/features/`

### Shell e navegação
| Componente | Props/Eventos | Descrição |
|------------|---------------|-----------|
| `app-shell` | getter `.outlet` | Layout raiz: `app-header` (topo) + `app-sidebar` (lateral) + outlet do roteador + **barra inferior** (`#bottombar`). Altura `100dvh`: o conteúdo rola no `main` → **sidebar/header/barra com altura constante**. No login esconde header+sidebar+barra; na rota pública (`/publico/*`) mostra o header (somente-leitura) **sem** sidebar **nem barra**. **Dock flutuante** (`#bottombar`, estilo iOS/Instagram — **cápsula ESCURA translúcida** via tokens `--dock-fundo`/`--dock-borda`/`--dock-ico`/`--dock-ico-fraco`/`--dock-ativo` + `--vidro-blur`, **totalmente arredondada** (`--raio-completo`), centralizada, flutua acima do conteúdo; **mobile E desktop**; só autenticado): Contatos · Empresas · Obras · Transferências · Orçamentos — **só ícones monocromáticos** (sem rótulo; `rotulo`→`title`/`aria-label`); o item **ATIVO** ganha uma **"pílula" clara/elevada** atrás (`--dock-ativo` + `--dock-ativo-sombra`), sem cores por item. A cápsula **segue o tema** (clara translúcida no claro · escura translúcida no escuro). **Ao rolar p/ baixo o dock apenas ENCOLHE** (`.reduzido` = `scale(.78)`, `transform-origin: bottom center`) e **continua visível/clicável** — volta ao tamanho ao subir/topo. Navega via `irPara` (SPA); `env(safe-area-inset-bottom)`; `:host([com-barra]) main` ganha folga inferior. **Preferência de exibição do dock** (export `dockPref`): ligar/desligar o dock **por dispositivo, separado p/ desktop e mobile** — padrão **desktop desligado / mobile ligado**; guardada em `localStorage` (`obras.dock.desktop`/`obras.dock.mobile`, local ao aparelho, como a preferência de recolher a sidebar). O `_refletirDock()` aplica `com-barra` conforme a largura atual + a preferência, reavaliando ao **cruzar o breakpoint** e ao evento **`dock-pref`** (emitido pelo Perfil → muda na hora, sem reload). **Modo `cheio`** (rota `/email`): `main` vira `overflow:hidden` e o dock some → a view ocupa 100% da altura sem barra de rolagem do navegador. |
| `app-header` | evento `toggle-sidebar` | Cabeçalho persistente (sticky). **Marca-bloco** com gaps iguais (logo↔texto == texto↔sanduíche): **logo** `src/assets/dattaobra.png` (transparente) + texto **"Dattaobra"** (link → `/obras`) + **botão sanduíche** (recolhe no desktop / drawer no mobile). O logo alinha com a coluna de ícones da sidebar e a largura do `nav` da sidebar (195px) é igualada a este cluster → o sanduíche encosta no limite direito da sidebar. À direita: **alternador de tema** (sol/lua), chip do usuário → `/perfil`, Sair. Ícones via `ui-icon`. **Modo somente-leitura** (link público, sem sessão): o MESMO componente renderiza só a marca + selo "Somente leitura" (sem menu/usuário/sair). **A marca (logo + "Dattaobra") foi movida p/ o topo do `app-sidebar`** — o header autenticado mostra só o **ícone de menu (☰)** à esquerda + tema/avatar/Sair à direita (header minimalista, formato app). `:host` em **liquid glass** (`--vidro-fundo-forte` + `--vidro-blur` + hairline inferior + sheen) sobre `env(safe-area-inset-*)` (status bar translúcida iOS) — botões/chip internos seguem sólidos. **Mobile (≤820px):** barra 56px, oculta nome/cargo do chip (só avatar); alvos de toque ≥ 44px. (Modo público mantém a marca no header — não há sidebar.) |
| `app-sidebar` | attr `aberto` (drawer mobile), `recolhido` (régua de ícones no desktop); evento `navegou` | Menu lateral em abas (Obras, **Empresas** (rota `/fornecedores`), Contatos, Cotações, Itens, **Transferências**, Perfil e, por último — abaixo de Meu perfil — **Configuração** via `role-guard` admin). **Rótulo "Empresas"** (antes "Fornecedores"): só o texto mudou — rota/id/ícone `fornecedor`/dados (`fornecedor_id`, `dataStore.fornecedores()`) seguem iguais. `template()` itera o array `ITENS`. O `<nav>` é **liquid glass** (`--vidro-fundo-forte` + `--vidro-blur` + hairline à direita). Ao recolher, os rótulos somem e o ícone fica no mesmo lugar; altura sempre 100% do conteúdo. Preferência persistida. **Marca (logo + "Dattaobra") no TOPO do `<nav>`** (link → `/obras`; recolhe para só o ícone no desktop recolhido). **Logo alinhada aos ícones do menu:** a imagem fica num **slot de 18px** (= largura dos `ui-icon` dos itens), a imagem em 22px centralizada (transborda simétrico, sem cortar) → o **centro da logo e o texto "Dattaobra" alinham exatamente** com os ícones/rótulos dos itens (expandido e recolhido). Espaçamento proporcional (topo `--esp-tela`; `margin-bottom` + gap do nav = 16px até o 1º item). **Drawer mobile (≤820px):** o `:host` cobre a viewport (acima do header) e quem desliza é o `<nav>`; o `.backdrop` cobre a tela toda → **tocar fora fecha**; fechado (`pointer-events:none`) deixa o conteúdo clicável; `env(safe-area-inset-*)` no `<nav>`. |
| `app-loader` | attr `texto` | Overlay de carregamento usado **só no boot sem cache** (refresh já logado). No fluxo de **login**, o carregamento acontece na própria tela de login (não há overlay). |
| `role-guard` | attr `role="admin"\|"usuario"` | Mostra/oculta o slot conforme o papel (UX). |

> As views leem do **data-store** ([data-store.js](../src/core/data-store.js)) — cache-first, sem recarregar. Navegação entre abas é instantânea.

> **Convenção de layout (distância padrão `--esp-tela`):** toda tela usa
> `.area { padding: var(--esp-tela); }` — **sem `max-width` e sem `margin: 0 auto`**.
> O conteúdo **preenche toda a largura** disponível e a distância é a MESMA em volta:
> **header→conteúdo == menu→conteúdo == borda direita→conteúdo** (= `--esp-tela`,
> hoje 24px), em qualquer largura. Quando a sidebar recolhe, o `main` alarga e o
> conteúdo acompanha automaticamente (largura fluida). O `app-sidebar` usa
> `--esp-tela` no topo → o 1º item do menu alinha com o topo do conteúdo. Nas telas
> com KPIs (detalhe da obra e da cotação) os KPIs são o **primeiro componente**.
> O reset do Shadow DOM ([base-element.js](../src/components/base-element.js)) zera
> margens (`* { margin: 0 }`, espelhando `reset.css`), então o 1º componente fica
> exatamente a `--esp-tela` do header em TODA tela (com ou sem KPIs).
> **Toda tela/aba nova deve seguir esse padrão** — inclusive a `publico-view`, que
> usa o `app-header` padrão (somente-leitura) e a mesma `--esp-tela` no conteúdo.

### Autenticação — `features/auth/`
| Componente | Props/Eventos | Descrição |
|------------|---------------|-----------|
| `login-view` | — | Tela `/login` **split-screen** (igual ao design): esquerda clara com a marca + `login-form`; direita verde-escura com card "Execução das obras" (sparkline SVG), título, 3 destaques e o fundo animado `login-grafo-bg`. Visual **fixo** (não segue tema). `< 900px` esconde o painel direito. |
| `login-form` | — | Formulário; campos próprios com ícone (e-mail/usuário, senha com olho), checkbox **"Manter-me conectado"** e botão "Entrar →" (`ui-button`, gradiente via `::part`). Chama `auth.login(email, senha, lembrar)`. **O carregamento acontece nesta tela** (o botão fica em loading até o snapshot chegar — ver [app.js](../src/app.js)); em sucesso a view é substituída ao navegar. Abaixo do "Entrar", um bloco **"ou / Entrar com Google"** (GIS) aparece só quando `CONFIG.GOOGLE_CLIENT_ID` está configurado e o script `gsi/client` carregou; o callback chama `auth.loginGoogle(idToken, lembrar)`. O link **"Primeiro acesso ou esqueci a senha"** abre `<redefinir-senha>` (e-mail pré-preenchido); se o login retornar `SENHA_NAO_DEFINIDA`, o fluxo abre **sozinho**. |
| `redefinir-senha` | props `.email`, `.contexto` ("login"\|"perfil"); eventos `concluido`/`fechar` | **Modal de 3 passos** (`ui-modal`) para **definir/redefinir senha via PIN por e-mail** — serve primeiro acesso, esqueci a senha (login) e trocar senha (perfil). **P1** e-mail → `auth.solicitarPin`; **P2** código de 6 caracteres (input próprio: maiúsculo, espaçado, `one-time-code`; "Reenviar") → `auth.confirmarPin` (guarda `resetToken`); **P3** nova senha + confirmar (`senhaMinima`) → `auth.definirSenha` → **login automático** via `auth.aplicarSessao` (no perfil, troca o token **sem** re-navegar). Indicador de passos + erros inline, sem libs. |
| `login-grafo-bg` | — | Fundo animado de "grafos" (canvas: ~52 nós à deriva + arestas) do painel direito do login. Respeita `prefers-reduced-motion`; pausa com a aba oculta; cleanup de `rAF`/observer via `aoLimpar`. |

### Perfil — `features/perfil/`
| Componente | Props/Eventos | Descrição |
|------------|---------------|-----------|
| `perfil-view` | — | Rota `/perfil`. Dados do usuário (do data-store) + card **"Aparência"** + segurança + card **"Conta Google"**. **Card "Aparência":** dois **`ui-switch`** para o **menu flutuante** (dock) — "no computador" e "no celular" — lidos/escritos via `dockPref` (import de `app-shell`); ao mudar, grava a preferência (localStorage) e o dock reage na hora (evento `dock-pref`). Padrão **desligado no computador / ligado no celular**. O estado da conexão vem **do cache** (`auth.config().google_conectado`, derivado no backend) — **não chama `google.status` ao abrir**. Conectar via popup OAuth + `postMessage`, testar conexão (`google.testarConexao`, sob demanda) e desconectar; conectar/desconectar atualizam a flag via `auth.setConfigLocal` (otimista). Quando conectado, dois **`ui-switch`** de sincronização (**Sincronizar agenda** / **Sincronizar notas**) lidos de `auth.config()`; ao ligar → `auth.definirConfig` + `sincronizarTodasObrasGoogle`. **Sincronizar contatos e empresas com o Google** (`sync_contatos`) — ao ligar, criar/editar contato/empresa e criar cargo espelham no Google Contacts (best-effort); importar/vincular manuais nas telas de Contatos/Empresas. |
| `senha-form` | — | Segurança (perfil): botão **"Alterar senha"** que abre `<redefinir-senha>` (contexto `perfil`, e-mail travado do próprio usuário) — a troca passa pelo **fluxo com PIN por e-mail** (mesmo do primeiro acesso). |

### Obras — `features/obras/`
| Componente | Props/Eventos | Descrição |
|------------|---------------|-----------|
| `obras-list-view` | — | Rota `/obras` (tela inicial). No topo, **`<painel-atencao>`**; abaixo, grid de cartões + criar/editar/excluir. |
| `painel-atencao` | — (auto-contido) | **"O que precisa de atenção"** no topo da tela inicial: cartões (gradiente, clicáveis via `<a href>`) para **Prazos** (obras não finalizadas atrasadas/vencendo em ≤7d → `/obras`; vermelho se há atrasada, senão laranja), **A pagar** (Σ `restoDespesa` das despesas em aberto → `/financeiro`) e **Cotações abertas** (com ofertas ainda não registradas → `/cotacoes`) + **lista das obras críticas** (nome + badge de prazo). Compõe `prazo-util` + `despesa-split`; assina o store e repinta. Some quando não há nada (mostra "Tudo em dia" se há obras). |
| `obra-card` | `.obra`; eventos `abrir`, `editar`, `remover` | Cartão com barra de orçamento (gradiente). **Altura fixa** (só varia na horizontal): título trava em 2 linhas (line-clamp). Ações via `ui-button`: Editar (`tonal`), Compartilhar (`secundario`), Excluir (`perigo-contorno`, ícone). **Badge de prazo** ("Faltam Xd"/"Vence hoje"/"Atrasada Xd") via `prazo-util`. |
| `obra-form` | `.obra`; eventos `salvo`, `fechar` | Modal criar/editar obra (chama a API). Inclui **Prazo de término** (`ui-input type=date`). |
| `obra-share-form` | `.obra`; evento `fechar` | Modal (só dono): **link público** curto (gerar/copiar/abrir/desativar) + **log de acessos** + convidar usuários para colaboração. |
| `financeiro-view` | — (rota `/financeiro`) | **Painel consolidado** entre todas as obras (compõe KPIs inline + `ui-tabs` + `ui-data-table`; reusa `despesa-split.balancos`). KPIs Total/Pago/Em aberto/Em pagamento; abas **A receber** (por destinatário real: Empresa p/ Material, Equipe/Contato p/ Serviço — sem dupla contagem), **A pagar** (por responsável), **Em aberto** (despesas com resto; clique → obra). 100% derivado. |
| `obra-detail-view` | attr `id` (rota); — | **Coração do tempo real**: no topo, **banner de contagem regressiva** do prazo ("Faltam X dias"/"Vence hoje"/"Atrasada X dias"/"Concluída", `prazo-util`) + botão **"Finalizar obra"/"Reabrir"** (`dataStore.atualizarObra({finalizada})` — para/retoma a contagem). KPIs + `ui-tabs` (**Gráficos** / **Despesas** / **Participantes** / **Responsáveis** / **Orçamentos** / **Equipes** / **Fornecedores** / **Transferência** / **Estoque** / **Agenda** / **Notas**); dashboard + despesas (otimista + cache). Aba **Despesas**: "+ Registrar Despesa" (`cotacao-despesa-form` obra-fixa). Aba **Orçamentos**: "+ Novo orçamento" abre `orcamento-form` com a obra **travada** + grade dos orçamentos da obra. Aba **Empresas** (rótulo renomeado; era "Fornecedores"): empresas usadas na obra + **WhatsApp** (`whatsappBtnHtml`) + Nº/Total/Recebido/**Saldo a receber** (`balancos.porFornecedor`; linha clicável → empresa). Aba **Transferência** → `ui-tabs` aninhado **[Transferências | Pagamentos]** que usa **os MESMOS cards da página /pagamentos do menu** (`montarGradeResumos` + `previaTransferenciaHtml`/`abrirTransferencia` e `previaPagamentoHtml`/`abrirPagamento`) — clique no card (ou no botão **Visualizar** do card) abre o banner; botão **"Enviar transferência"** abre o `pagamento-form`; a **exclusão** (cascata) é feita pelo botão "Excluir transferência" **dentro do banner** `abrirTransferencia`. Aba **Estoque** → `ui-tabs` aninhado **[Estoque | Consumidos]**: tabelas (`ui-data-table` fluido/clicável) com Item · Classificação (badge) · Categoria (badge ou "Sem categoria") · Unidade (⚠ se divergente) · Em estoque/Consumido — consolidação **derivada** dos movimentos pelo helper puro `estoque.js`. **Clique na linha** → banner `abrirOrigemEstoque` (vinculos.js) mostrando a **ORIGEM** do item (de quais despesas veio, manual ou transferência). Ação por linha: **Reduzir** (Estoque → consumir) / **Aumentar** (Consumidos → devolver) via banner `ui-input` numérico com **LIMITE** (item 10: reduzir ≤ em estoque; aumentar ≤ consumido). Botões no cabeçalho: **"+ Adicionar manual"** (modal `ui-select` item + qtd/unidade → `adicionarEstoqueManual`, item 15) e **"Transferir"** (modal item + obra destino + qtd → `transferirEstoque`; o destino registra a obra de origem, item 14). Tudo via `dataStore.consumir/devolver/adicionarManual/transferirEstoque` (write-through + `EVENTOS.ESTOQUE`; `subscribe` re-renderiza). **(Repasse foi removido da UI — a divisão entre integrantes é feita pela "Distribuição entre integrantes" no pagamento.)** Na aba **Despesas**, ao SELECIONAR despesas (barra de seleção): **Lançar pagamento** (abre `pagamento-form` com as selecionadas; ignora as já **QUITADAS** — usa `restoDespesa > 0.01`, o **mesmo critério visível** do form/status) e **Definir responsabilidade**. **As ações em massa re-resolvem as despesas FRESCAS por id no store (`_frescas`)** antes do guard — a seleção da `ui-data-table` é por referência de objeto e pode ficar desatualizada após uma mutação; isso evita falso "já quitada"/"já tem pagamento". **Exclusão × estoque (itens 18/19/20):** `_bloqueioEstoqueDespesa(d)` — se a despesa virou estoque (`entrada_despesa`) e parte já foi CONSUMIDA (`em_estoque < qtd da entrada`), `remover`/`removerMassa` bloqueiam com `avisar` **detalhado** (em estoque agora · qtd da despesa · falta devolver) apontando **Estoque › Consumidos** (botão Aumentar); se couber (não consumida, ou consumo já devolvido), exclui e o backend remove a entrada (reduz o saldo). O backend (`despesasRemover`) tem a MESMA trava (autoritativa). |
| `obra-agenda` | attr `obra-id` | Aba **Agenda** = **centro de tudo** da obra. Agrega as **marcações datadas do app** (`marcacoes.js`: despesas, transferências/pagamentos, notas, **prazo** — cor por tipo) e **FUNCIONA SEM Google**. Se a conta Google estiver conectada, soma os **eventos avulsos** do Google (filtra os sincronizados, `ev.sincronizado`, p/ não duplicar) e permite criá-los/editá-los (`agenda-evento-form`). Duas vistas: **Calendário** (`agenda-calendario`) e **Agenda** (`agenda-lista`, linha do tempo estilo Google Calendar). Clique numa marcação do app → **`marcacao-detalhe`** (modal com os dados completos + "Abrir na aba de origem", que troca a aba via `#abas` no mesmo shadow root); evento Google → editar. **Cache-first:** renderiza **na hora** (marcações do data-store + eventos avulsos do Google do `localStorage`, chave `dattaobra.agenda.google.<obraId>`) — sem spinner e sem esperar rede; a conexão vem de `googleConectado()` (cache). Em **segundo plano** re-lista o Google e repinta. Se `sync_agenda` ligado, sincroniza a obra 1× ao abrir + botão **"Sincronizar agora"** (`sync-agenda.js` → `google.agenda.sincronizarObra`). Reage a `dataStore.subscribe`. |
| `agenda-cores.js` / `marcacoes.js` / `sync-agenda.js` / `prazo-util.js` / `nota-cores.js` | (helpers) | `agenda-cores`: cores do Google (colorId→hex; `corEvento` aceita hex). `marcacoes`: `marcacoesDaObra(obraId)` deriva marcações do data-store. `sync-agenda`: `sincronizarObraGoogle`/`sincronizarTodasObrasGoogle` (push idempotente) + `googleConectado()`. `prazo-util`: dias/status/texto/cor do **prazo** da obra (reusado no detalhe, no card e nas marcações). `nota-cores`: paleta pastel das notas (`PALETA_NOTA`, `fundoNota/tintaNota/bordaNota`) — usada por `nota-card` e `nota-form` (espelha a allowlist do backend). |
| `agenda-calendario` | prop `.eventos`; eventos `novo`/`dia-novo {data}`/`evento-abrir {evento}` | **Grade visual** do calendário (imita o Google Calendar): cabeçalho (mês/ano, ‹ Hoje ›, Mês/Semana, + Novo evento), **Mês** (6×7, hoje destacado, chips coloridos por `colorId`, "+N mais", clique no dia → painel do dia) e **Semana** (7 colunas). Componente PURO (não toca em rede). Responsivo: no mobile o Mês vira **pontos** + painel do dia; a Semana empilha. Cores em `agenda-cores.js`. |
| `agenda-lista` | prop `.eventos`; eventos `novo`/`evento-abrir {evento}` | Vista **"Agenda"** (schedule) imitando o Google Calendar: linha do tempo **cronológica ascendente**, com **faixa de mês** (gradiente sazonal), **cabeçalho de intervalo de semana** e uma **linha por dia** (dia da semana + número à esquerda; **barras coloridas** do evento à direita; hora em eventos com horário). Componente PURO (não toca em rede). "Hoje" destacado; responsivo. |
| `marcacao-detalhe` | props `.marcacao`, `.obraId`; eventos `fechar`/`ir-aba {aba}` | Modal (`ui-modal`) com os **dados completos** de uma marcação do app, buscados do data-store por `ref`: **despesa** (item, classificação, valor, data, pago, observação, autor), **transferência** (valor, data, tipo, observação, autor), **nota** (texto completo, autor, datas), **prazo** (obra, prazo, situação via `prazo-util`). Rodapé: **"Abrir na aba de origem"** (emite `ir-aba` → `obra-agenda` troca a aba) + Fechar. Reusa `moeda`/`data` (formatters). |
| `agenda-evento-form` | props `.obra` ({id,nome}), `.evento` (editar/pré-preencher) | Modal (`ui-modal`) com **todos os campos do Google Calendar**: título, **dia inteiro** (alterna `date`↔`datetime-local`), início/fim, **repetir** (`ui-select` RRULE), **lembrete**, **local**, **convidados** (e-mails), **cor** (11 do Google) e descrição → `google.agenda.criar`/`atualizar`; emite **"salvo"/"fechar"**. Espelha `tipo-transf-form`. |
| `obra-notas` | attr `obra-id` | Aba **Notas** da obra — anotações **compartilhadas** (quem tem acesso à obra vê/edita), em **cards coloridos** (estilo Google Keep): `ui-card mesa` + **grade masonry** (`column-width`, responsiva) de `nota-card`. "+ Nova nota" / clicar / editar abre `nota-form`; excluir com confirmação. Lê `dataStore.notasDaObra(obraId)` e reage a `dataStore.subscribe`. |
| `nota-card` | `.nota`; eventos `abrir`/`editar`/`excluir` | Cartão de nota estilo Keep: fundo colorido por `nota.cor` (`nota-cores.js`), título, prévia do texto, rodapé (autor + data) e ações Editar/Excluir (ícones, ≥44px no mobile). Clique no card → `abrir`. |
| `nota-form` | props `.obraId`, `.nota` (editar) | Modal (`ui-modal`) título + **textarea** + **seletor de cor** (swatches da `PALETA_NOTA`, ≥44px) → `dataStore.adicionarNota/atualizarNota` (envia `cor`); emite **"salvo"/"fechar"**. |
| `obra-participantes` | attr `obra-id`, `modo` (participantes\|responsaveis) | Aba de participantes (modelo paga ↔ recebe): colunas **Pago · Recebido · Saldo a pagar · Saldo a receber** (`despesa-split.balancos`) + painel **"quem deve a quem"** (`despesa-split.acerto`). modo `participantes`: todos + adicionar/remover; modo `responsaveis`: só os marcados. **Linha clicável** → tela do contato (`/contatos/:id`, resolvido pela chave `c:<id>`) ou da equipe (`/equipes/:id`); coluna de **WhatsApp** (`whatsappBtnHtml`, telefone do contato). |
| `participante-form` | `.obraId`; eventos `salvo`, `fechar` | Modal `ui-select` p/ adicionar um **contato** cadastrado como participante. |
| `responsaveis-form` | `.obraId`; evento `fechar` | Modal p/ marcar, entre os participantes, quem são **responsáveis** (alterna o flag via `dataStore.definirResponsavel`). |
| `publico-view` | attr `token` (rota `/publico/:token`) | Visão **pública somente-leitura** (sem login) que traz a **obra inteira, com TODAS as abas** num `ui-tabs`: **Gráficos** (`dashboard-summary` + `category-breakdown`) · **Despesas** (itens) · **Acerto de contas** (balanços por participante + "quem deve a quem", via os helpers PUROS `balancos`/`acerto`) · **Orçamentos** · **Equipes** · **Fornecedores** (`balancos.porFornecedor`) · **Transferências**. **Sem nenhuma ação de edição** (tabelas sem `.acoes`, sem botões de criar) — é um componente **ISOLADO** (não reusa `obra-detail-view` → zero risco à tela autenticada). Resolve nomes (contato/equipe/participante/fornecedor) por **mapas próprios do payload** (`publico.obra` devolve `despesasRaw` + `participantes` + `categorias` + `fornecedores/contatos/equipes` **só os referenciados** + `orcamentos/transferencias/pagamentos` da obra). **Sem header próprio** — usa o `app-header` padrão (modo somente-leitura) montado pelo `app-shell`, que mostra o header (sem sidebar/dock) também nas rotas `/publico/*`. |

### Despesas — `features/despesas/`
| Componente | Props/Eventos | Descrição |
|------------|---------------|-----------|
| _(removido)_ `despesa-form` | — | O antigo `despesa-form` foi removido. A despesa nasce SEMPRE do **registro de uma oferta**. O **cadastro manual** voltou como o modo **"Uma despesa nova"** do `cotacao-despesa-form` (obra-fixa): cria item + oferta inline e registra — tudo num só lugar, reusando `criarOferta`/`registrarDespesaOferta` (não usa `despesas.criar`). |
| `despesa-table` | `.despesas`, `.categorias`, `.participantes`; eventos `abrir` (clique na linha), `editar`, `remover`, `excluir-massa`, `acao-massa` | **Responsivo:** **desktop = `ui-data-table`** (abaixo); **mobile (≤820px) = `ui-lista-gestos`** — cards enxutos (Item · Valor · Empresa/Ofertante · badges Classificação+Status · data), **sem legendas/checkbox/botões**. Gestos: tocar→`abrir`, arrastar→=editar, arrastar←=excluir (→`remover`; usa o fluxo com trava de estoque + confirmação — despesa não tem desfazer), segurar→seleção → `acoesMassa` (Registrar pagamento / Definir responsabilidade / Excluir). **Emite os MESMOS eventos** da versão desktop → `obra-detail-view` não muda. A tabela **full-width** e fluida; **no mobile (≤820px) só Data/Item/Classificação/Valor/Status** (demais marcadas `secundaria`). Colunas: Item (largura mín. 280px), **Classificação** e **Categoria**, **Ofertante** (`ofertanteNome`) e **Empresa**, Adicionado/Editado por + **Status** (`statusPagamento`), **Pagamento**, **Distribuição**, **Responsabilidade** e **Oferta** (id de `preco_id` — vincula à oferta de origem). Reusa `ui-data-table` + `category-badge`. |
| `pagamento-form` | `.obra`, `.despesasSelecionadas`, `.aviso`; eventos `salvo`, `fechar` | **Banner (modal)** p/ registrar uma **TRANSFERÊNCIA** que agrupa N pagamentos (1 por despesa): lista as despesas com saldo (checkbox + alocação, default=resto, total ao vivo), **Quem pagou** (participante + fallback usuário/contatos), **Tipo** (dinheiro/crédito/débito/boleto) e data (máx=hoje) → **1 chamada** `dataStore.lancarTransferencia`. **Pré-valida a regra de ouro** (`transferencia-regra.recebedorUniforme`): se as marcadas têm recebedores/empresas diferentes, mostra `ui-alert` e **desabilita** o salvar (o backend revalida). Quando há **1 despesa marcada cujo recebedor é uma EQUIPE**, mostra **Distribuir entre integrantes (R$)** (`split-editor` com os integrantes da equipe — `integrantesDaEquipe`) e envia `distribuicao` junto (mesmo critério que o backend aplica: `alocacoes.length===1 && recebedor_equipe_id`). Igual à seção da despesa. **Comprovante (opcional):** campo `<input type=file>` (PDF/imagem, ≤10MB, lido em base64 via FileReader) enviado em `comprovante:{base64,nome,mime}` no `lancarTransferencia` → salvo no Google Drive (`Drive.gs`). Título do modal: "Enviar transferência". |
| `repasse-form` | `.pagamento`, `.obra`; eventos `salvo`, `fechar` | **Banner (modal)** p/ registrar um **repasse** de um pagamento: contatos a repassar (checkbox) + valor + data → `dataStore.lancarRepasse`. |
| `pagamento-util.js` | [features/pagamentos/pagamento-util.js](../src/features/pagamentos/pagamento-util.js) | `previaPagamentoHtml(p)` — card de prévia do pagamento (esverdeado; mostra **também a transferência: tipo + total** via `transferenciaDoPagamento`; e, quando há **distribuição entre integrantes**, a linha "Distribuído: Nome (R$)"). `abrirPagamento(p)` — **banner** com valor/data/pagou/recebedor/empresa/obra, um **bloco da TRANSFERÊNCIA em CINZA ESCURO** (clique → `abrirTransferencia`), **despesas cobertas**, **Distribuição entre integrantes (N)** (lista nome+valor de `p.distribuicao`, quando recebedor é equipe; nomes via `nomeChavePart`). **(A seção de repasses foi REMOVIDA — só a distribuição.)** `previaTransferenciaHtml(t)` — card da transferência em **tom INFO (azul)** (era cinza), com **chip "📎 comprovante"** quando há anexo. `abrirTransferencia(t)` — **banner** da transferência (valor total em `--cor-info`/tipo/pagou/recebedor/empresa/obra + seção **Comprovante**: **pré-visualização EMBUTIDA** do documento — **imagem** vira `<img>` (thumbnail do Drive `…/thumbnail?id=…`, leve/confiável no MOBILE) e **PDF** vira `<iframe>` `…/d/<file_id>/preview` — mostra o documento no próprio banner (responsivo: 380px desktop / 300px mobile) + ações **Abrir em nova aba** (link `…/view`) / **Substituir** / **Remover**, ou **Anexar comprovante** quando não há — via `dataStore.anexarComprovanteTransferencia`/`removerComprovanteTransferencia` + reabre o banner; arquivo lido por `_lerArquivoBase64`/`_escolherEAnexarComprovante`) + os **pagamentos agrupados** (clique → `abrirPagamento`); botão **Excluir transferência** via `excluirTransferenciaComAviso`. **`montarGradeResumos(el, itens, classe, previaHtml, abrir, vazio)`** — renderiza a **grade de cards-resumo** (injeta o próprio `<style>`; cada card tem botão **"Visualizar"** com `stopPropagation`, alvo ≥44px no mobile); componente ÚNICO reusado pela **página /pagamentos** E pela **aba Transferência da obra**. `TIPOS_TRANSFERENCIA`, `nomeTipo`, `nomeContato/nomeEquipe/nomeChavePart/nomeRecebedor`. |
| `transferencia-regra.js` | [features/pagamentos/transferencia-regra.js](../src/features/pagamentos/transferencia-regra.js) | **Regras puras** (testáveis sem DOM; espelham `_recebedorDaDespesa` do backend): `chaveRecebedor(despesa)`, `recebedorUniforme(despesas)` (regra de ouro: mesmo recebedor+empresa), `totalAlocacoes(alocacoes)`. **Testes:** `test/transferencias.test.mjs`. |
| `ui-lista-gestos` | props `.itens=[{id,...}]`, `.render(item)→html`, `.acoesMassa=[{nome,rotulo,variant?}]`, `.letraDe(item)→"A"`; attr `indice` | `abrir`, `editar`, `excluir` ({item}), `selecao` ({itens}), `acao-massa` ({acao,itens}) | **Lista MOBILE por GESTOS** (estilo app nativo; primitivo sem regra de negócio). O consumidor dá os itens + a função que monta cada linha; a lista cuida dos gestos (ponteiro): **tocar** → `abrir` (ou alterna a seleção); **segurar ~450ms** → modo **seleção** (círculo de check + barra flutuante com `acoesMassa`/Excluir/Cancelar); **arrastar →** (verde `--grad-verde` + `editar`) → `editar`; **arrastar ←** (vermelho `--grad-vermelho` + `excluir`) → `excluir`. Linhas usam `touch-action:pan-y` (rolagem vertical nativa) e dão `stopPropagation` no início do arraste horizontal → **vencem o swipe de aba** do `ui-tabs`. Com `indice`, agrupa por `letraDe` e mostra a barra **A–Z** à direita (tocar rola até a seção). Usado no mobile por `despesa-table`, `contatos-view`, `fornecedores-view` (o desktop segue com `ui-data-table`). |
| `pagamentos-view` | rota `/pagamentos` (menu lateral, rótulo **Transferências**) | `ui-tabs` **[Transferências | Pagamentos]** renderizadas via **`montarGradeResumos`** (pagamento-util): transferências como cards **azul/info** (`previaTransferenciaHtml` → `abrirTransferencia`) e pagamentos como cards esverdeados (`previaPagamentoHtml` → `abrirPagamento`). O MESMO helper/visual é usado na aba Transferência da obra. |
| `pagamento-form` (multi-despesa) | + prop `.despesasSelecionadas`, `.aviso` | Lista as despesas (marcáveis, valor=resto, total ao vivo) + **Quem pagou** + **Data** (máx = hoje; não aceita futura). **NÃO** pede recebedor/empresa — cada despesa mostra "recebe: <ofertante>" e o pagamento é registrado **por despesa** (1 por despesa via `lancarPagamento`; recebedor/empresa = ofertante/fornecedor da despesa). `.despesasSelecionadas` lista só essas, já marcadas; `.aviso` mostra um `ui-alert` no topo (ex.: "N já têm pagamento e foram ignoradas") — sem `confirm()` nativo (1 banner só). |
| `despesa-detail` | `.despesa`, `.categorias`; evento `fechar` | **Banner (modal)**. Despesa de **oferta** (`preco_id`): mostra a **oferta de origem** pelo **mesmo card de prévia** (`previaOfertaHtml`); **clicar abre** o **banner único da oferta em só-leitura** (`abrirOferta(oferta,{somenteLeitura:true})`). Para editar, edita-se a OFERTA na origem. + Data. **Sem editor de categoria** (vem do item). Seção **Pagamentos**: badge de **status** + Pago/Resto, os pagamentos como **cards esverdeados** (`previaPagamentoHtml`; clique → `abrirPagamento`; ✕ exclui e **desvincula** via `dataStore.excluirPagamento` — funciona p/ a ENTIDADE Pagamento **e** p/ a leva embutida antiga). Os pagamentos vêm de `dataStore.pagamentosDaDespesa` (UNIFICADO: entidades + sintetizados das levas). Permite **vários pagamentos PARCIAIS até QUITAR** — cada um vira **uma transferência (1 pagamento)**; o form de lançar fica visível enquanto houver **saldo** e **some só quando quitada** (`restoDespesa ≤ 0,01`), **sugerindo o saldo restante** como valor padrão. Cada card de pagamento mostra **também a transferência (tipo + valor total)**. Mini-form **Lançar pagamento** numa **caixa visualmente separada** (`.lancar`: recuada na cor `--cor-mesa`, borda + título "Lançar pagamento") com os **mesmos campos do `pagamento-form`** (mesma população: **Quem pagou** = participante + fallback usuário/contatos via `ui-select`; **Tipo** lido de `tiposTransferencia` via `nomeTipo`; valor + data; `split-editor` de integrantes p/ equipe) → `dataStore.lancarPagamento` (cria 1 transferência/1 pagamento, **mesmo backend** do `pagamento-form`). O "quem pagou quanto" é **derivado das levas**. Também editável: observação e **Responsabilidade** (`split-editor`). Legada (sem `preco_id`): item/valor editáveis. **Regras** (`ui-alert`): quem pagou obrigatório; Σ % ≤ 100; leva ≤ resto; Σ distribuição ≤ leva. |
| `split-editor` | `.participantes`, `.itens=[{chave,valor}]`, `.modo("valor"\|"pct")`, `.limite`; evento `mudar` | Editor reutilizável de **distribuição entre participantes** (linhas `ui-select`+`ui-input`, total/soma com `.limite` = valor ou 100%; destaca quando excede). Usado p/ pagamento (R$), responsabilidade (%) e recebido por integrante (R$). |
| `despesa-split.js` | `parseLista`, `totalPago`, `distribuicao`, `rotuloOrigem`, `acerto`, `totalRealizado`, `restoDespesa`, `statusPagamento`, `balancos`, **`balancosDePagamentos`** | Helpers puros: **acerto** "quem deve a quem"; pagamentos parciais (status/realizado/resto); **`balancos`** (modelo paga ↔ recebe, lê o ESPELHO embutido da despesa) e **`balancosDePagamentos(despesas, pagamentos)`** (mesmo shape, mas lê a COLEÇÃO de Pagamentos — entende pagamento que cobre VÁRIAS despesas; equivale a `balancos` p/ despesa única). **Testes:** `node --test` (`test/despesa-split.test.mjs`, `test/pagamentos.test.mjs`). |
| `exportar-tabela.js` | `gerarCSV`, `gerarXLS`, `gerarXLSX`, `gerarPDF`, `gerarConteudo`, `nomeArquivo`, `baixarTabela(formato, {titulo,colunas,linhas})` | **Exportação de mesa** (CSV/XLS/XLSX/PDF) **SEM dependências externas** (mantém o vanilla): CSV (RFC 4180, `;` + BOM); **XLS = BIFF8 binário real dentro de contêiner OLE2/CFB** (abre LIMPO no Excel, sem o aviso "formato e extensão não conferem" — substituiu o antigo HTML-as-xls; células de texto em LABEL Latin-1, 16 XF, BOUNDSHEET→offset da planilha); **XLSX OOXML real** (ZIP "stored" + CRC32 próprios); **PDF 1.4 real** com fonte **Courier (monoespaçada)** → largura exata por caractere e truncagem por coluna (nada fora da célula), paisagem A4, paginação. Geração PURA (string/Uint8Array) — `baixarTabela` é o único que toca o DOM (Blob + download). Consumido pelo `ui-data-table` (barra Exportar). **Testes:** `test/exportar-tabela.test.mjs` (inclui validação estrutural OLE2/BIFF8). |
| `estoque.js` | `consolidar`, `consolidarObra`, `emEstoqueDaObra`, `consumidosDaObra`, `saldoItem`, `limiteConsumo`, `limiteRetorno`, `unidadesDoItem`, `origensDoItem` | **Camada PURA** do estoque (livro-razão de movimentos): consolida por `(obra,item)` → `adquirido/consumido/em_estoque` (derivado, sem contador mutável). `limiteConsumo`(=em_estoque) / `limiteRetorno`(=consumido) p/ as travas (item 10); `origensDoItem` lista as ENTRADAS (item 17); `unidadesDoItem` sinaliza unidades divergentes. **Testes:** `test/estoque.test.mjs` (consolidação, limites, exclusão 18/19, bordas). Consumido pelos getters `dataStore.estoqueDaObra/estoqueConsumidoDaObra/origensDoEstoque/saldoEstoqueItem`. |
| `category-badge` | `nome`, `cor` | Reutiliza `ui-badge`. |

### Itens — `features/itens/` (+ `features/categorias/categoria-form.js`)
| Componente | Props/Eventos | Descrição |
|------------|---------------|-----------|
| `itens-view` | — | Rota `/itens`. `ui-tabs`: **Itens** (catálogo; badge Material/Serviço + coluna **Categoria**; **linha clicável → `/itens/:id`**; CRUD via `item-form`) e **Categorias** (só `tipo:"item"` via `dataStore.categoriasItem()`; **todas editáveis**, reusa `categoria-form`). Ambas mostram **log** (Criado em/Atualizado em + autor/editor) via `colunasLog()`. |
| `item-detail-view` | attr `id` (rota `/itens/:id`) | Página do item (espelha `fornecedor-detail-view`): **faixa de KPIs** (Total gasto · Despesas · Cotações · Obras; cards `--grad-*`) + cabeçalho (nome + badge classificação + "Editar item") + `ui-tabs` **Despesas** (de todas as obras; clique → obra), **Cotações** (clique → cotação; melhor preço via `melhorTotal`), **Obras** (agrega nº+valor gasto por obra; clique → obra). 100% client-side (lê `item_id` do store). |
| `item-form` | `.item`; eventos `salvo`, `fechar` | Modal criar/editar item (nome + `ui-select` Classificação Material/Serviço + `ui-select` **Categoria OBRIGATÓRIA** = `categoriasItem()`). A categoria só é definida/alterada aqui (propaga p/ oferta → despesa). Emite `EVENTOS.ITENS`. **Ao criar, se já existe item com o mesmo nome → `avisarDuplicado` (banner) e o usuário escolhe continuar.** |
| `shared/duplicado.js` | `avisarDuplicado(tipo, nome, existentes)` → Promise&lt;boolean&gt; | Helper: se `existentes` já tem um registro com o mesmo `nome` (case-insensitive), mostra `confirmar()` ("Já existe … chamado X. Cadastrar assim mesmo?"); sem duplicado → `true`. Usado por `item-form`/`contato-form`/`fornecedor-form` **só ao criar**. |
| `shared/drop-crud.js` | `editarEntidade(tipo, valor, onSalvo)`, `excluirEntidade(tipo, valor, onFeito)`, `ehPrimario(tipo, record)` | **Editar/Excluir INLINE** a partir dos ícones do `ui-select` (eventos `editar`/`excluir`). `tipo` ∈ `item`/`contato`/`fornecedor`/`cargo`/`classificacaoFornecedor`/`classificacaoItem`. **Editar** abre o form da entidade (item/contato/fornecedor/cargo/categoria-form) com o registro. **Excluir** REUSA `abrirBannerVinculos` + `vinculosDo*` (`vinculos.js`) → **bloqueia se houver vínculo** (ofertas/despesas/obras/contatos/…) mostrando onde; se livre, `confirmar()` + `dataStore.remover*` (o backend também trava). **`ehPrimario`**: cargo `fixo` / classificação `GLOBAL` → sem ícones (protegido). Os pais adicionam `editavel/removivel` nas options (false p/ primários) e ligam os eventos. |
| `categoria-form` | `.categoria`, **`.tipo`** (`item`\|`fornecedor`); eventos `salvo`, `fechar` | Modal criar/editar `categoria` (nome + cor). Rótulo agora é **"Categoria"** para ambos os `.tipo` (`item` e `fornecedor`); envia `tipo` ao criar. Emite `EVENTOS.CATEGORIAS`. (Antes: "Subclassificação"/"Classificação".) |

> Ao criar/editar uma classificação, `EVENTOS.CATEGORIAS` faz a `obra-detail-view`
> recarregar as categorias (atualiza o select de despesa e os rótulos da tabela).

### Dashboard — `features/dashboard/`
| Componente | Props | Descrição |
|------------|-------|-----------|
| `dashboard-summary` | `.resumo` | KPIs em **cartões com gradiente + ícone** (total, orçamento, saldo, qtd); reusa `ui-icon` e tokens `--grad-*`. **Cartões CLICÁVEIS** (`<button>` + "ⓘ"): o clique abre `ui-modal` com a **origem do número** + composição (`_detalhe`: por classificação/categoria do `resumo`, ou Orçamento−Total=Saldo). **O breakdown lê o campo `total`** dos itens do resumo (`{nome,cor,total}`; `valor` é fallback) — "Total gasto" = soma do **valor das despesas registradas** (não confundir com pago/realizado). |
| `category-breakdown` | `.porCategoria`; attr `titulo` | Barras (rola na vertical). Título configurável (`titulo`, padrão "Gastos por categoria") p/ reuso: obra/público = **"Gastos por categoria"** (`por_subclassificacao`); cotação = comparativo por contato. |
| `grafico-rosca` | `.porCategoria`; attr `titulo` | Donut (SVG) **sem número central** + legenda. Título configurável; na obra = **"Distribuição por classificação"** (`por_classificacao` = Material/Serviço). |
| `grafico-mensal` | `.despesas` | Barras por mês; **rola na horizontal** com muitos meses. |

> Os 3 gráficos ficam em `ui-card` de **tamanho igual** (grade 1×3, altura fixa);
> cada um preenche o cartão e rola internamente. A view do detalhe usa `ui-tabs`
> (**Gráficos** / **Despesas**) abaixo dos KPIs.

### Compras — `features/fornecedores/`, `features/contatos/`, `features/cotacoes/`
Módulo de cotações: agenda de **fornecedores** (empresas) e **contatos** (pessoas);
**cotações** comparam ofertas de contatos e a melhor pode virar despesa numa obra.
Tudo lê do data-store (cache-first) e emite `EVENTOS.FORNECEDORES/CONTATOS/COTACOES`.

| Componente | Props/Eventos | Descrição |
|------------|---------------|-----------|
| `fornecedores-view` | — | Rota `/fornecedores` (rótulo de UI **"Empresas"**). `ui-tabs` **[Empresas \| Classificação]**. **Empresas** (responsiva via `matchMedia`): **desktop = `ui-data-table`** (linha clicável → página da empresa); **mobile (≤820px) = `ui-lista-gestos` estilo telefone** — avatar + nome + subtítulo (categoria) + WhatsApp, ordem alfabética com **índice A–Z**; tocar → `/fornecedores/:id`, arrastar→ = editar, arrastar← = excluir (`excluirEntidade` `semConfirmacao` → vínculos + Desfazer), segurar → multi-excluir. CRUD (nome, telefone, e-mail, classificação). **Classificação**: gerencia as classificações de empresa — `categoria` com **`tipo:"fornecedor"`** (pool **distinto** da categoria de itens; `dataStore.categoriasFornecedor()`), reusando `categoria-form` (prop `.tipo="fornecedor"`) + `dataStore.*Categoria` + banner `vinculosDaSubclassificacao`. Se o Google está conectado, botão **"Importar do Google"** (→ `dataStore.importarGoogle("fornecedor",...)`). _(Termo de UI = "Empresa(s)"; código/rota/eventos/`fornecedor_id` inalterados.)_ |
| `fornecedor-detail-view` | attr `id` (rota `/fornecedores/:id`) | Página da empresa: cabeçalho + `ui-tabs` com **Contatos**, **Ofertas**, **Orçamentos** e **Dados** (por obra: **Recebido / Saldo a receber**; clique → obra). Aba **Contatos**: **"+ Novo contato"** (abre `contato-form` já vinculado a esta empresa → aparece também em Contatos) **e "+ Vincular contato"** (banner inline `ui-modal`+`ui-select` que liga um contato EXISTENTE via `atualizarContato({fornecedor_id})`). **Ações Google** no topo (se conectado): selo "Vinculada ao Google" + Vincular / Enviar·Atualizar / Abrir / Desvincular (mesmo padrão de `contato-detail-view`, `tipo="fornecedor"`). |
| `fornecedor-form` | `.fornecedor`; eventos `salvo`, `fechar` | Modal criar/editar **empresa** (nome*, telefone, e-mail, cnpj, classificação, observação). **Classificação** via `ui-select` (busca) com **"+ Cadastrar classificação"** inline (`categoria-form` tipo=fornecedor → seleciona a nova). **Ao criar, avisa se já existe empresa com o mesmo nome** (`avisarDuplicado`). |
| `contatos-view` | — | Rota `/contatos`. `ui-tabs`: **Contatos**, **Equipes** (grade de `equipe-card` + "+ Nova equipe") e **Cargos** (fixos + extras, CRUD via `cargo-form`). A aba **Contatos é responsiva** (`matchMedia`): **desktop = `ui-data-table`** (tabela clicável); **mobile (≤820px) = `ui-lista-gestos` estilo telefone** — avatar (`avatarHtml`) + nome + subtítulo (cargo · empresa) + WhatsApp, em ordem alfabética com **índice A–Z**; **tocar** → `/contatos/:id`, **arrastar→** = editar (`editarEntidade`), **arrastar←** = excluir (`excluirEntidade` com `semConfirmacao` → banner de vínculos + Desfazer), **segurar** → multi-seleção → excluir em massa. Se o Google está conectado, botão **"Importar do Google"** (abre `google-contato-picker` → `dataStore.importarGoogle("contato",...)`). |
| `contato-detail-view` | attr `id` (rota `/contatos/:id`) | Página do contato: `ui-tabs` — **Obras**, **Empresa** (se vinculado a uma empresa), **Equipes**, **Ofertas**, **Orçamentos** e **Dados** (por obra: **Pago · Recebido · Saldo a pagar · Saldo a receber**; clique → obra). **Ações Google** no topo (se conectado): selo "Vinculado ao Google" + **Vincular** (picker) / **Enviar**·**Atualizar no Google** / **Abrir no Google** / **Desvincular** (`dataStore.enviarGoogle/vincularGoogle/desvincularGoogle`). |
| `contato-form` | `.contato`; eventos `salvo`, `fechar` | Modal criar/editar. **Cargo** e **Empresa** via `ui-select` (busca) com **"+ Cadastrar cargo"** (`cargo-form`) e **"+ Cadastrar empresa"** (`fornecedor-form`) **inline** — ao salvar, o novo já é selecionado no campo. Empresa **visível p/ QUALQUER contato** (vínculo opcional via `fornecedor_id`; **obrigatório só p/ Vendedor**). (O campo "superior" do Pedreiro foi **removido** — agora é via Equipes.) **Ao criar, avisa se já existe contato com o mesmo nome** (`avisarDuplicado`). Ao salvar, com `sync_contatos` ligado, o `data-store` espelha no Google (best-effort). |
| `cargo-form` | `.cargo`; eventos `salvo`, `fechar` | Modal criar/editar **cargo extra** (nome). Os 6 obrigatórios são fixos. Ao criar, com `sync_contatos` ligado, cria a **classificação (grupo)** no Google. |
| `google-contato-picker` | props `.titulo`; eventos `escolher`({contato}), `fechar` | Modal (`ui-modal largo`) para **escolher um contato do Google** (People API) — usado em Importar e Vincular. Carrega via `dataStore.listarContatosGoogle(q)` com **busca no servidor** (debounce); clique numa linha escolhe. Estado de erro amigável se o Google não estiver conectado (com permissão de contatos). |
| `cotacoes-view` | — | Rota `/cotacoes`. Tabela `clicavel` de cotações — **1 cotação por categoria** (modo único; o backend faz get-or-create e a migração unifica duplicatas, então cada subclasse aparece **uma vez**). Linha enxuta: **Categoria · Ofertas (nº) · Melhor oferta (unit.) (menor `valor unitário` final entre as ofertas, via `cotacao-util.melhorUnitario`) · Criado em · Atualizado em** (`colunasLog`). Criar uma cotação leva (`irPara`) à cotação da subclasse (nova ou já existente). Orçamentos e Ofertas saíram daqui para abas próprias do menu. |
| `ofertas-view` | — | Rota `/ofertas` (aba do menu). Lista **todas** as ofertas (`montarTabelaOfertas` + `dataStore.todasOfertas()`) + "+ Criar oferta" (`preco-form` avulsa). |
| `cotacao-form` | `.cotacao`; eventos `salvo`, `fechar` | Modal criar/editar. **Modo ÚNICO: sempre por SUBCLASSIFICAÇÃO** (não há mais toggle nem campo de item — cada oferta define o próprio item dentro da subclasse). Campos: **Categoria** (obrigatória), quantidade, unidade, **obra opcional**, status. |
| `cotacao-detail-view` | attr `id` (rota `/cotacoes/:id`) | Faixa `<oferta-kpis>` + `ui-tabs` **[Gráficos \| Ofertas]**. Título = **nome da categoria**. **Ofertas AGRUPADAS por item** (`dataStore.precosDaCotacaoPorItem`): **uma "mesa" (`ui-card`) por item**, com nº de ofertas e **melhor preço do grupo** no título, ofertas ordenadas do menor total p/ o maior; colunas padrão (`colunasOferta`) **sem Item/Categoria** (viram o título da seção). Por grupo: Escolher/Editar/Excluir + **Registrar como despesa** + edição/exclusão em massa; `+ Adicionar oferta` (preco-form com item restrito à subclasse). Tudo numa tela só. |
| `oferta-kpis` | `.resumo={num,menor,media,maior,economia}` | KPIs das ofertas em cartões com gradiente (reusa o estilo do `dashboard-summary`). |
| `grafico-evolucao-precos` | `.historico`, `.cotacao`, `.contatos`, `.cores` | **Gráfico de linhas (SVG), uma linha por contato** — evolução do preço no tempo a partir do histórico; legenda por contato. |
| `preco-form` | `.cotacao`/`.cotacaoId`, `.orcamento`, `.preco`, `.somenteLeitura`; eventos `salvo`, `fechar`. Export **`abrirOferta(oferta, {somenteLeitura?,cotacao?,orcamento?})`** | **Banner ÚNICO da oferta** — **criar / editar / ver detalhes** no mesmo componente (mesclou o antigo detalhe). O **ITEM** é um **card clicável** (nome + classificação + categoria) → abre banner de **detalhes do item** (`abrirDetalheItem`, com link p/ `/itens/:id`); só vira **select** ao **criar sem cotação**. Demais campos: ofertante (contato/grupo), fornecedor (auto-preenchido), qtd, valor, desconto, **prazo (obrig.)**, obs. **Travas por origem:** `.somenteLeitura` (aberto pela despesa) → tudo só-leitura (item card + campos em linhas, sem Salvar); `.cotacao` → item fixo; `.orcamento` → ofertante/fornecedor travados. Regras: Material → fornecedor obrigatório; Serviço → ofertante obrigatório. Ofertas legadas pré-preenchem item/qtd pela cotação. `abrirOferta(...)` é o ÚNICO opener (clique em qualquer oferta abre este banner). Chama `criarOferta`/`atualizarOferta`. |
| `cotacao-despesa-form` | `.cotacao`+`.preco` (oferta-fixa) **ou** `.obraFixaId` (obra-fixa); eventos `registrado`, `fechar`. Export **`abrirRegistrarDespesa(oferta)`** | Banner ÚNICO **"Registrar Despesa"** (mesmo em qualquer lugar). **oferta-fixa** (`.preco`): escolhe a **obra** (resumo da oferta, item próprio mesmo sem cotação). **obra-fixa** (`.obraFixaId`, na obra): "O que registrar?" → **Uma oferta** · **Uma despesa nova** · **Orçamento completo**. **Uma despesa nova** (manual, tudo num só lugar): **Item / Ofertante / Empresa** são `ui-select` (busca) e cada um tem **"+ Cadastrar"** inline — **item** (`item-form`), **contato** (`contato-form`), **empresa** (`fornecedor-form`) — que ao salvar **selecionam o novo** registro no campo (ofertante/empresa por classificação — Material→empresa, Serviço→ofertante, com auto-empresa pelo contato). Preenche os dados da **oferta** (quantidade, valor, desconto, prazo, obs; mesmas regras/validação do `preco-form`) + prévia do total; ao confirmar, **cria a oferta avulsa (`criarOferta`) e registra a despesa (`registrarDespesaOferta`) em um passo** — mesma lógica existente. Sem categoria (herdada do item). `abrirRegistrarDespesa(oferta)` é a **entrada padrão do botão "Registrar"** em toda tabela de ofertas (ignora ofertas já registradas). Chama `registrarDespesaOferta`/`registrarOrcamentoCompleto`. Responsivo (linhas empilham ≤560px). |
| `cotacao-util.js` | `totalOferta`, `totalOfertaCheio`, `qtdOferta`, `unitFinalOferta`, `melhorTotal`, **`melhorUnitario`** (menor valor unitário final), `resumoOfertas`, `coresPorContato`, `PALETA_CONTATOS` | Funções puras. **Valor final** = `(valor_unit_desconto||valor_unit) × (preco.quantidade||cotacao.quantidade)`; `totalOfertaCheio` = sem desconto; `qtdOferta`/`unitFinalOferta` helpers; menor total/resumo p/ KPIs (pelo valor final); cor estável por contato. |

**Orçamentos — `features/orcamentos/`** (container de ofertas de várias cotações)
| Componente | Props/Eventos | Descrição |
|------------|---------------|-----------|
| `orcamento-card` | `.orcamento`; eventos `abrir`/`editar`/`remover` | Card quadrado (espelha `obra-card`): título (ou rótulo automático), badge do **tipo** (Material/Serviço), **Total** das ofertas + nº, fornecedor/contato + obra, log. |
| `orcamento-form` | `.orcamento`, `.obraFixaId`; eventos `salvo`, `fechar` | Modal: título (opc), **tipo** (Material/Serviço), **fornecedor** (só Material), **ofertante** (Material → contatos do fornecedor; **Serviço → contatos OU equipes**, valores `c:`/`e:`), **obra** (opc). Com `.obraFixaId` (aberto pela aba Orçamentos da obra) a obra é **travada** (campo somente-leitura, não alterável). |
| `orcamentos-view` | — | Rota `/orcamentos` (aba do menu). Grade de `orcamento-card` (`montarGradeOrcamentos`) + "+ Novo orçamento". |
| `orcamento-detail-view` | attr `id` (rota `/orcamentos/:id`) | Cabeçalho + resumo (nº · total) + **tabela PADRÃO de ofertas** (`colunasOferta`); "+ Adicionar oferta" abre o banner `preco-form` (escolhe o **item**; ofertante/fornecedor herdados do orçamento). |
| `orcamento-util.js` | `rotuloOrcamento`, `totalOrcamento`, `ofertanteNome`, `colunasOferta`, **`montarTabelaOfertas`**, `colunasOrcamento`, `COR_CLASSIFICACAO` | **`colunasOferta()`** = colunas PADRÃO da oferta (Item, Classificação, Categoria, Ofertante, Fornecedor, Qtd, Valor unit., **Unit. c/ desc.** (verde), Total, **Total c/ desc.** (verde), Prazo, Cotação, Orçamento, **Obra**, **Despesa** (id), **Status** — menos críticas `secundaria`). **Obra/Status** derivam da despesa vinculada (`despesa.preco_id == oferta.id` ou `oferta.despesa_id`): Status = `statusPagamento` (A pagar/Em pagamento/Pago); **Despesa** = id da despesa. **`montarTabelaOfertas(el, ofertas, {acoes,onAcao,onLinha,semRegistrar})`** monta a tabela padrão e **inclui a ação "Registrar"** (→ `abrirRegistrarDespesa`; suprimida por `semRegistrar`); reuso em cotação/orçamento/fornecedor/contato/Ofertas. **`previaOfertaHtml(oferta)`** = card de prévia da oferta (item/badge/valor/qtd×unit/ofertante·empresa — usado no banner Registrar Despesa e dentro da despesa). _(O detalhe da oferta foi mesclado no `preco-form` via `abrirOferta`; o antigo `abrirDetalheOferta` foi eliminado.)_ |
| `orcamento-grade.js` | `montarGradeOrcamentos(el, lista)` | Renderiza uma **grade de `orcamento-card`** com abrir/editar/excluir — reusada em `orcamentos-view`, fornecedor/contato/obra. |

> A **oferta é única** (`CotacaoPrecos`+`orcamento_id`): aparece na cotação E no orçamento.
> As abas **Ofertas** (fornecedor/contato detail) usam `colunasOferta()` (mesma tabela
> das cotações); as abas **Orçamentos** (fornecedor/contato/obra) usam
> `montarGradeOrcamentos()` (grade de cards). O contato detail tem abas **Ofertas** e
> **Orçamentos** separadas.

**Equipes — `features/equipes/`** (grupo: líder + membros + obras; espelha Orçamentos)
| Componente | Props/Eventos | Descrição |
|------------|---------------|-----------|
| `equipe-card` | `.equipe`; eventos `abrir`/`editar`/`remover` | Card quadrado: nome, líder, nº membros, nº obras, log. |
| `equipe-form` | `.equipe`; eventos `salvo`, `fechar` | Modal: **nome** + **líder** (`ui-select` filtrado a Mestre de Obra/Engenheiro/Gestor). |
| `equipe-detail-view` | attr `id` (rota `/equipes/:id`) | Cabeçalho + `ui-tabs` **Obras / Membros / Dados** (cada aba = `ui-card` + `ui-data-table`). Vincular obra / adicionar membro pelo botão "+" no `slot="acoes"` do card → **banner flutuante** (`ui-modal` + `ui-select` composto inline, sem componente novo). Dados: **tabela** (1 linha por despesa da equipe) com **Data · Obra · Item · Quem pagou · Data do pagamento · Pago · Saldo a receber** (clique → obra). Salva via `dataStore.atualizarEquipe`. |
| `equipe-util.js` | `CARGOS_LIDER`, `liderNome` | Cargos elegíveis a líder; nome do líder ao vivo. |
| `equipe-grade.js` | `montarGradeEquipes(el, lista)` | Grade de `equipe-card` (mesmo layout de Orçamento) — reusada nas abas Equipes de contatos/contato-detail/obra. |

> A antiga lógica "Pedreiro → superior" foi **removida**; o Pedreiro agora pertence a
> uma ou mais **Equipes**. `contato-detail` mostra as equipes do contato (líder/membro).

> As tabelas de fornecedores, contatos, cotações e ofertas mostram a coluna
> **"Criado em"** (campo `criado_em`); o detalhe da cotação mostra "Criada em …".

### Admin — `features/admin/`
| Componente | Props/Eventos | Descrição |
|------------|---------------|-----------|
| `email-view` | — | Rota `/email` (**só admin**). Cliente de e-mail em **3 colunas** (Pastas \| Lista \| Leitura), **ocupa toda a altura** (o `app-shell` põe `/email` em modo **`cheio`** → `main` sem rolagem + dock oculto). **Cache-first + prefetch** (`email-cache`): abre **instantâneo** e, ao entrar, faz **prefetch em 2º plano de TODAS as pastas** (Entrada/Enviados/Com estrela/Rascunhos/Lixeira via `_prefetchOutras`, pulando as já em cache) → trocar de aba **não tem espera**; ao abrir cada pasta, refresh em 2º plano. **Seleção múltipla** (checkbox por item + **"Selecionar todos"** no cabeçalho da lista → barra: **Arquivar / Excluir / Marcar lida / Não lida** em lote via `email.caixa.marcarVarios`). **Pastas:** Entrada/Enviados/Com estrela/Rascunhos/Lixeira + **Marcadores**. **Lista:** avatar, remetente, hora, assunto, **estrela**, prévia, busca. **Barra do topo**: busca (esq.) + **Escrever/Configurar/Atualizar** (dir.). **Responsivo** (desktop 3 col; **tablet/mobile** oculta a coluna de pastas → **seletor de pastas** (`select`) na barra do topo; mobile 1 col + leitura em tela cheia com "Voltar"). |
| `email-conversa` | props `.threadId`, `.assunto`; eventos `compor {modo}`/`mudou`/`voltar`/`novo` | Painel de leitura **inline** (não-modal) da 3ª coluna (`email.caixa.ler`), **cache-first** (`emailCache.getConversa` → reabrir e-mail já lido é **instantâneo**, atualiza em 2º plano). Barra (assunto + estrela), **marcadores**, rodapé **Responder / Resp. todos / Encaminhar / Arquivar / Excluir**. **Ocupa todo o espaço**: a mensagem aberta cresce e o corpo (`<iframe sandbox srcdoc>`, anti-XSS) **preenche a altura** (sem altura fixa). **Thread com várias mensagens (estilo Gmail):** mensagens **recolhidas** (cabeçalho + resumo); a **última abre por padrão** ocupando o espaço; clicar num cabeçalho recolhido abre aquela e recolhe a anterior; clicar na aberta **recolhe** (volta a escolher) — **uma aberta por vez** (accordion). Anexos clicáveis (baixam via Blob) só na aberta. Sem `threadId` → estado vazio. Marca lida ao abrir. (Substitui o antigo `email-leitura` modal.) |
| `email-compositor` | props `.modo`, `.threadId`, `.draftId`, `.para/.cc/.bcc/.assunto/.corpoHtml` | Modal **editor profissional** (estilo Quill): cabeçalho **Enviar/Cancelar/Anexar + arraste**; **De** (alias/endereço com **nome de exibição**); **Para/Cc** = `email-campo-destinatarios` (**chips** avatar+nome+×, autocomplete + botão que abre o **banner** `email-picker-contatos` p/ escolher **vários**); fontes: você + usuários/compartilhados + contatos + **empresas** (`dataStore.usuario/usuarios/contatosAtivos/fornecedoresAtivos`). **Cco/Assunto**; **toolbar** (B/I/U/tachado/limpar, **dropdown de parágrafo** Normal/H1/H2/H3, **cor** e **realce** em **paleta popover**, listas, **alinhamento e link em ícone SVG/`ui-icon`** — sem emoji — via `execCommand` no **mousedown+preventDefault**); **anexos** (clique+arraste); **auto-salvar rascunho** + **Descartar**. |
| `email-enderecos` | eventos `fechar` | Modal (admin): **criar/remover endereços** `@dattaobra.com.br` com **nome de exibição**; endereços da conta (principal+aliases); **assinatura em editor rico** (`email.caixa.assinatura`) + guia da infra (Cloudflare Routing + Resend). |
| `email-cache.js` | módulo | Cache leve da caixa (Gmail é ao vivo → fora do snapshot). `caixas`: lista de **TODAS as pastas** (Entrada/Enviados/Estrela/Rascunhos/Lixeira/marcadores) espelhada em `localStorage` (instantâneo entre reloads). `conversas`: corpo já lido por `threadId` (**só memória** — corpos grandes) → reabrir e-mail instantâneo. API: `getLista/setLista/invalidar`, `getConversa/setConversa/invalidarConversa`, `getRemetentes/setRemetentes`. Padrão **cache-first** (abre instantâneo, refresh em 2º plano); invalidado nas ações. |
| `admin-view` | — | Rota `/admin` (só admin), rótulo **"Configuração"** no menu (abaixo de Meu perfil). `ui-tabs` **[Usuários | Transferências | Sistema]**: Usuários = CRUD de usuários; **Transferências** = CRUD dos **tipos de transferência** (4 base fixos como badges de referência + extras do usuário em tabela com Editar/Excluir; "+ Novo tipo" abre `tipo-transf-form`); **Sistema** = `sistema-email-config` (remetente das notificações/recuperação de senha). Espaço pensado p/ crescer. |
| `sistema-email-config` | — | Card em **Configuração › Sistema** (admin): define o **remetente** das notificações do sistema (recuperação de senha) via `admin.email.obter`/`admin.email.definir` (Script Property `EMAIL_REMETENTE`). Mostra para onde vai a **cópia (BCC)** das recuperações (a caixa `dattaobra@gmail.com`) — visível no Gmail e na aba **E-mail**. |
| `tipo-transf-form` | `.tipo` (edição); eventos `salvo`/`fechar` | **Modal** criar/editar um TIPO de transferência extra (ex.: Pix) → `dataStore.criarTipoTransferencia`/`atualizarTipoTransferencia`. Espelha `cargo-form`. |
| `components/confirmar.js` | — | `confirmar(opts)`→Promise&lt;boolean&gt; e `avisar(opts)`→Promise&lt;void&gt; (só OK). **Modal de aviso/confirmação reutilizável** (`ui-modal`+`ui-button`, primitivo — sem regra de negócio). **Substitui 100% dos `confirm()`/`alert()` nativos do sistema** (inclui a exclusão em massa do `ui-data-table`); aceita `listaHtml` (ex.: pagamentos que serão excluídos), `perigo`, `rotuloOk`/`rotuloCancelar`, `soOk`. |
| `users-table` | `.usuarios`; eventos `editar`, `config` | Reutiliza `ui-data-table`. |
| `user-form` | `.usuario`; eventos `salvo`, `fechar` | Modal criar/editar usuário (**sem campo de senha** — o usuário a define no primeiro acesso via PIN). Criar = `{nome,email,role}` + dica; editar = `{nome,role,ativo}`. |
| `user-config-form` | `.usuario`; evento `fechar` | Modal de configurações (chave-valor). |

---

### Helpers compartilhados (não são componentes)
| Helper | Onde | O que faz |
|--------|------|-----------|
| `colunasLog()` | [core/audit-columns.js](../src/core/audit-columns.js) | Devolve as 2 colunas padrão de **log** (Criado em + autor / Atualizado em + editor) para qualquer `ui-data-table`. Usado por itens/fornecedores/contatos/cotações + detalhes; `obra-card` mostra o log no rodapé; `users-table` mostra Criado em/por. |
| `avatar.js` | [features/shared/avatar.js](../src/features/shared/avatar.js) | **Avatar único** de contato/fornecedor (iniciais, círculo, cor estável por nome). `avatarNomeHtml(nome)` (avatar + nome) e `avatarHtml(nome,tam)`/`corAvatar(nome)`/`iniciais(nome)`. Fonte ÚNICA do avatar em TODO o sistema — usado no `app-header` (chip do usuário), `contatos-view`, `fornecedores-view`, participantes/responsáveis e fornecedores da obra (`obra-participantes`, `obra-detail-view`) e membros/“quem pagou” de equipe (`equipe-detail-view`). **Sem duplicar** markup (o `.avatar` inline do header foi eliminado). Também exporta **`whatsappBtnHtml(telefone, tam)`** — botão de WhatsApp (link `wa.me`) que acompanha contato/empresa em todo o sistema (listas + cabeçalho do detalhe, ao lado de Editar; **também nas abas Participantes/Responsáveis e Empresas da obra**); **inativo/cinza** quando não há telefone. Não aparece no header do app. |
| `rastreabilidade.js` | [features/shared/rastreabilidade.js](../src/features/shared/rastreabilidade.js) | **Camada PURA** (sem browser, testável) de derivação REVERSA das vinculações — "quem aponta p/ X" só a partir dos FKs diretos (nada duplicado). `rastrearContato/Fornecedor/Item/Subclassificacao/Equipe/Obra/Oferta(id, ctx)` → listas vinculadas (inclui **pagamentos/repasses**); `obraIdDaOferta(oferta, ctx)` resolve a obra em cascata (própria→cotação→orçamento, cobre avulsas). `ctx` = coleções simples. Testes: `test/rastreabilidade.test.mjs`. |
| `vinculos.js` | [features/shared/vinculos.js](../src/features/shared/vinculos.js) | `vinculosDoItem/Fornecedor/Contato/Subclassificacao/Cargo/Oferta/`**`Obra`**`/`**`Equipe(...)`** montam os grupos (colunas+rota) **delegando à camada `rastreabilidade.js`** (monta o `ctx` do store). Cobertura ampliada (ex.: fornecedor→ofertas/despesas/obras; contato→despesas/equipes/obras; item→ofertas/obras). `abrirBannerVinculos({titulo,grupos,aoExcluir})` abre o **banner** (`ui-modal`+`ui-alert`+`ui-data-table`+`ui-button`) e **bloqueia a exclusão**; sem vínculos, executa `aoExcluir`. Também `abrirOrigemEstoque({tituloItem,origens,obraId})` — banner read-only com a **origem** de um item do estoque (despesas/manual/transferência; item 17). |
| `edicao-massa.js` | [features/shared/edicao-massa.js](../src/features/shared/edicao-massa.js) | `editarEmMassa(linhas, {criarForm, reler, aplicar, ignorar?})` — edição em massa "modo planilha" REUSANDO o MESMO form de edição da linha: abre o form da 1ª selecionada e, ao `salvo`, calcula os **campos que mudaram** (comparação semântica: ignora 1↔"1", undefined↔"") e aplica **só eles** às demais via `aplicar(linha, diff)`. Ligado por quem tem `editar-massa` (ofertas, fornecedores, contatos, itens, cotações, usuários). |

> **Valores ao vivo:** as tabelas resolvem o nome ATUAL da entidade vinculada pelo
> `id` (`dataStore.item(id)` etc.) — o campo denormalizado é só fallback. Renomear
> reflete em todas as telas. (Participantes e visão pública re-derivam no backend.)

## Como reutilizar / criar um componente

1. Estenda `BaseElement` e implemente `estilos()` e `template()`.
2. Use `aoConectar()` para listeners e `aoLimpar(unsub)` para limpeza automática.
3. Para um primitivo, **não** importe nada de `core/` (exceto utilitários puros)
   nem de `features/`.
4. Emita resultados via `this.emitir("nome", detalhe)`.
5. `customElements.define("minha-tag", MinhaClasse)` no fim do arquivo.
